specVersion: 0.0.4
schema:
  file: ./schema.graphql
dataSources:
  - kind: near
    name: receipts
    network: near-testnet
    source:
      accounts:
        suffixes:
          - "contract3.lpos-market.testnet"
          - "contract3.restaking-base.testnet"
      startBlock: 135579806
    mapping:
      apiVersion: 0.0.6
      language: wasm/assemblyscript
      entities:
        - ExampleEntity
      receiptHandlers:
        - handler: handleReceipt
      file: ./src/mapping.ts
