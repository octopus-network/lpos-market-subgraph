specVersion: 0.0.4
schema:
  file: ./schema.graphql
dataSources:
  - kind: near
    name: receipts
    network: near-testnet
    source:
      accounts:
        suffixes:
          - "contract-2.lpos-market.testnet"
          - "contract-2.restaking-base.testnet"
      startBlock: 138508317
    mapping:
      apiVersion: 0.0.6
      language: wasm/assemblyscript
      entities:
        - ExampleEntity
      receiptHandlers:
        - handler: handleReceipt
      file: ./src/mapping.ts
